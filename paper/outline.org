* Old
** Abstract
- what markov disccovery is
- why markov blanket discovery is useful
- however hard problem, large search space
- previous approaches avoid this large search space
- but with the trade off of not being able to capture
- certain higher order relationship between variables.
- most notably, as far as we are aware, existing methods
- are not capable of consistently discovering a simple xor relationship
  between just 3 variables
- Therefore in this paper, we revisit the possibility of exact markov
  blanket discovery 
- By modifying the existing IAMB method for markov blanket
  discovery resulting in our approach called LIAM-MB.
- Specifically we will additional step after the forward selection phase
  of IAMB
- where we will use techniques common in frequent itemset mining to
  search the subset lattice of all possible expansions of the current
  candidate markov blanket from phase 1.
- Experiments on synthetic Bayesian networks containing simple xor and
  parity relationships shows that LIAM-MB is able to capture those
  relationships while other approaches are unable to.
- Of course, the exact nature of LIAM-MB comes at a cost; further
  experiments on common benchmark datasets will show how LIAM-MB
  compares to existing Markov blanket discovery approaches
- Further experiments on common benchmark datasets will show the cost of
  the exact nature of LIAM-MB, paticularly when compared to IAMB
** Intro
- What is it
  + find markov blanket
  + origins
- Motivation: usecase
- Why is it hard
- Generally how methods avoid hardness
- trade off: some high order relationships no captured
- xor mini-experiment
** Related Work
- Recent Review: [cite:@yu2020a]
- Best method for xor so far: [cite:@gao2017]
- G2 test: [cite:@tsamardinos2006]
** Notations and definitions and Background
*** G Test
*** MB discovery and IAMB
- one of the first approaches to mb discovery that scales up well to
  thousands of variables
- prior to this work, the main other algorithm capable of mb discovery
  that scales well is the grow-shrink algo
- both approaches use a two phase approach to mb discovery
  + the first phase being one where they admit variables into a
  + cmb based on some statistical test (forward selection)
  + and second being one where they prune (backward pruning)
- The main difference...
- Thanks to the heuristic nature
  + are fast and scales well 
- the problem with this and similar approaches....
- In this paper, we will build upon IAMB directly
  + by inserting an exhaustive search phase
  + between "forward" and "prune"
** Method: LIAM-MB
- our approach builds upon the 2 step approach of IAMB
  + Specifically we want to find variables in the MB
  + that the first phase of IAMB did not
- Therefore, after the first phase we will carry out
  + a theorectically exhaustive search over
  + all possible subsets $S\in X - MB - t$
  + and check for conditional independece of $S$ with $t$
  + given MB
- The total number of subsets that will be in this search space is
  + ....
- Therefore One benefit of carrying out the exhaustive search
  + after the first phase
  + is that the larger the candidate MB found by phase 1
  + the larger
- 
*** Set enumeration tree
- Since the search space is the powerset of $X - MB - t$
- 
- A = vars in descendents
- if traversed via a depth-first search
- gurantees on the subsets relationship with one another
*** Bound on G-Statistic
- 
*** Pruning
**** Why limit df when upper bound test fails
- higher df => harder test to pass
- => any subset will fail any test at or above this df
- => we should only visit nodes with df less
- => only visit easier tests
**** early return from failed upper bound g-test
*** Complexity
- based on the max G-stat and df needed to *not* reject hypothesis
** Experiments
- Throughout this section we will compare
  + our appproach
  + to other approach
- description of each method
- pycausalfs implementation of methods
- alpha = 0.01
- recall as a measure of acc
** Experiments with Synthetic Data
- In order to empirically determine situations where
  + a heuristic approach to MB discovery might not capture 
  + in this section
- Specifically the boolean function we will use are:
  + parity
  + and / or
  + exactly-k
  + prime
- A bn with the nodes and connectivity in fig
  + CPT based on the boolean function, 1 for pr=1 when func is truee
- We use pgmpy to construct
  + sample 10 sets of 500 samples using bn forward sample in pgmmpy
  + seed used during sampling is the i-th sample
- We then tasked them to learn the mb of each var
- we then report the recall
** Experiments with Benchmark Data
- To further compare our approach 
- as a sanity check
- commonly used benchmark data
  + used in....
- data found at [link]
- we use 5000 sample versions
- results averaged over 10 runs
- find the MB of the top 10 variables with the largest MB
** Future work
- other methods for lattice mining step
- better bounds on G-stat or use of another test with better bounds
- add-on for other methods
- multiple test correction
- other metrics
* New
- link k-assoc with cliques
- we need to sort "to add" at each iter
- a notation of k-assoc
  + that is compatible with previous notions of 2/3-assoc
- for k-assoc we need to consider assoc between 2 vars
  + via some set of vars
- how does limited samples affect faithfulness
- 2-assoc requires conditioning on entire cmb to work
  + also requires assumption 1
  + to prevent spouses from canceling each other
  + does cancellation result in higher order assocs?
** Intro
** Probabilistic Graphical Models and Independece
** The Faithfulness Assumption
** When is Faithfulness Violated
*** High order interactions
*** When samples are limited
** kOMB: An algorithm for high-order markov blanket discovery
*** What would be nice in theory
**** Algorithm
1. Sort \(A\) by ascending p-value
2. 
3. find $\mathbf{X}\subset\mathbf{V}$ s.t.
   $\mathbf{X}$ and $Y$ are strictly \(k\)-associated
   - done by first finding $\mathbf{X}$ where its loosely
     \(k\)-associated, i.e. combination of lower order 
4. BFS to find minimal subset of \(\mathbf{X}\) 
**** Proof

*** What we need to do with limited samples
*** Statistical Test
** Pruning the Search Space of kOMB
- We found that it reduced the visited nodes by x%
** Experiments
*** Parity
*** Known Benchmarks
**** at different k orders
